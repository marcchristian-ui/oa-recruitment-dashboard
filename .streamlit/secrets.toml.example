# Streamlit Cloud Secrets — copy the values into share.streamlit.io app settings.
# Do NOT commit the real secrets.toml — it's already in .gitignore.

MANATAL_API_TOKEN = "paste-your-manatal-token-here"
MANATAL_API_BASE = "https://api.manatal.com/open/v3"
